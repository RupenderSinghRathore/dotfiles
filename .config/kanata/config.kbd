;; defsrc is still necessary
(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps h m '
  ralt
  lctl
)
(defvar
  tap-time 0
  hold-time 200
)

(defalias
  ;;escctrl (tap-hold 0 $hold-time esc lctl)
  quotectrl (tap-hold 0 $hold-time ' rctl)
)

(deflayer base
  lctl h m @quotectrl
  lalt
  caps
)
